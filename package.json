{
  "name": "exif-editor",
  "repository": "https://github.com/fragment0/exif-editor.git",
  "author": "fragment0",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build:shared": "cd packages/shared && npm run build",
    "web": "npm run build:shared && cd packages/exif-editor-web && npm run dev"
  },
  "private": true
}
